/** Mismo check que `GoalList.tsx`: stroke oscuro a propósito, para que se lea sobre el fondo verde del `.goal__box` marcado (currentColor quedaría claro-sobre-claro). */
export function GoalCheckIcon() {
  return (
    <svg width="12" height="12" viewBox="0 0 12 12" fill="none">
      <path d="M1.5 6.4 4.4 9.3 10.5 3" stroke="#06281c" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round" />
    </svg>
  )
}
